apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: enterprise-system-004608-peyg-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: enterprise-system-004608-peyg.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: enterprise-system-004608-peyg-service
            port:
              number: 80
